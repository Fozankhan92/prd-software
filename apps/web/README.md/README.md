# Desktop shell UI

This package is not the product itself and is not a standalone web app. It is the embedded React and TypeScript interface packaged inside the PRD Software Tauri 2 desktop application.

Native commands, local persistence, secure storage, filesystem integration, and operating-system behavior belong to the Rust desktop layer in src-tauri. The shipped product is the installable PRD Software desktop application for Windows, macOS, and Linux.
